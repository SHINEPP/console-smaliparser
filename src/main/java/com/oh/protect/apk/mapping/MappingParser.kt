package com.oh.protect.apk.mapping

import java.io.File
import java.nio.file.Files


object MappingParser {

    fun test() {
//        // Path to the mapping.txt file
//        val mappingFile = File("path/to/mapping.txt")
//
//        // Load the mapping file
//        val lines = Files.readAllLines(mappingFile.toPath())
//
//        // Create a ReTrace instance to parse the mapping file
//        val builder: ReTraceCommand.Builder = ReTraceCommand.builder()
//            .mapping(lines)
//            .regularConsumer(System.out::println)
//            .verboseConsumer(System.err::println)
//
//        val reTrace: ReTrace = ReTrace(builder.build())
//
//
//        // Example of retracing a class name (as it would appear in stack traces)
//        reTrace.retraceClass("a.b.c")
    }
}